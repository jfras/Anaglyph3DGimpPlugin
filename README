Gimp python plugin

1. Create image composed of two layers: 
  * Top layer - left eye, red colour. 
  * Bottom layer - right eye, greem colour.

[2. - optional - Point 4 points using path tool:
   * first key point on the top layer - layers will be translated to match this point on both layers,
   * second key point on the top layer - layers will be rotated to level this point on both layers
   * second key point on the bottom layer,
   * first key point on the bottom layer]
    
3. Select "Create 3D" option from "3D Tools" menu.
   
